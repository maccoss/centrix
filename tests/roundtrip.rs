// Integration test: Phase 1 identity passthrough.
// Once the mzML reader/writer are fully implemented, this will verify that
// all scan numbers and native IDs are preserved and the output parses cleanly.

#[test]
fn placeholder_compiles() {
    // Phase 1 stub — real roundtrip tests added when mzdata reader is wired up.
}
